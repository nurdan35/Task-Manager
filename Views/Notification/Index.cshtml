@model List<TaskManagement.Models.Notification>

<h2>Notifications</h2>
<ul>
    @foreach (var notification in Model)
    {
        <li>
            <strong>@notification.NotificationDate:</strong> @notification.Message
        </li>
    }
</ul>